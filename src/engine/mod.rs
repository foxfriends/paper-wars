use crate::state::State;

/// An instance of the game engine. Each engine runs a single game.
pub struct Engine {
    // TODO: what is the contents?
}

impl Engine {
    /// Creates a new engine given its state
    pub fn new(_state: State) -> Self {
        Self {}
    }
}
